💜push() 배열의 마지막에 값을 넣어줌
💜pop() 배열의 마지막 값을 삭제
💜shift() 배열의 첫 번째 값을 삭제
💜unshift() 배열의 첫 번째에 값을 넣어줌
💜splice()
(start, num, '추가할 값')
몇 번째 인덱스 부터 몇개를 지울건지. 그리고 시작 인덱스 자리에 뭘 추가할 건지(마지막은 선택)
reverse() 배열의 인덱스 순서를 반대로 뒤집어줌

변수 선언에 접근할 땐 지역변수에서 먼저 찾는다. 지역변수에서 찾는 값이 없다면 전역변수의 값의 출력한다.
